import { PageTitle } from '../../../_metronic/layout/core';

export function Profile() {
    return (<>
        <PageTitle breadcrumbs={[]}>
            Мой профиль
        </PageTitle>

        Здесь будет профиль юзера
    </>);
}